# images/

Curated runtime definitions: the CUDA OCI images (`base-cuda` / `train` / `serve-vllm`)
and the macOS/MLX venv-bundle pipeline ([ADR-0015](../docs/adr/0015-pluggable-compute-backends.md)).
Three images, not eight — Phase 1 scope is defended viciously
([build README §1, hard call #4](../docs/build/README.md)). Lands with **PR-24**.
